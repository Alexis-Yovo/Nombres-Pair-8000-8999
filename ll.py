# Boucle pour parcourir les nombres de 8000 à 8999
for nombre in range(8000, 9000):
    # Vérifier si le nombre est pair en utilisant l'opérateur modulo (%)
    if nombre % 2 == 0:
        # Si le nombre est pair, l'afficher
        print(nombre)
